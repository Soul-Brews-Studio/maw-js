{
  "name": "watch",
  "version": "1.0.0",
  "entry": "./index.ts",
  "sdk": "^1.0.0",
  "description": "Activity worklog — desync-killer timeline (tool-calls + PR status).",
  "author": "kobo:core",
  "cli": {
    "command": "watch",
    "help": "maw watch <claim|release|log|inject|sync> — setup-hooks moved to 'maw company hooks repair'",
    "hidden": true
  },
  "hooks": {
    "serve": {
      "script": "./serve.ts",
      "handler": "serve",
      "ensures": ["http:route:/api/worklog"],
      "policy": "best-effort"
    }
  },
  "weight": 10,
  "license": "MIT",
  "schemaVersion": 1
}
