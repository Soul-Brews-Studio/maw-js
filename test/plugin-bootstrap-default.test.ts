// The canonical plugin-bootstrap regression suite lives beside the module for
// historical #817/#1015/#1339/#1484 context. The default coverage command only
// runs files under test/, so import it here to keep that coverage visible.
import "../src/cli/plugin-bootstrap.test";
